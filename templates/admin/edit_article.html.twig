{% extends 'admin/base.html.twig' %}

{% block title %}{{ parent() }}Modification d'Article{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="my-3 col-md-8 offset-md-1">
                <h1 class="m-4">Modification de l'article {{ article.title }}</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-outline-primary">Mise à jour de l'article</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}