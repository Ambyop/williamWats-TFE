{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}Contact{% endblock %}

{% block body %}
    <main class="container">
        <section class=row">
            <div class="col-md-8 offset-2">
                <h2 class="my-3">Remplissez le formulaire</h2>
                {{ form_start(form) }}

                {{ form_widget(form) }}
                <button type="submit" class="btn btn-outline-secondary">Envoyer le mail</button>

                {{ form_end(form) }}
            </div>
        </section>
    </main>
{% endblock %}
